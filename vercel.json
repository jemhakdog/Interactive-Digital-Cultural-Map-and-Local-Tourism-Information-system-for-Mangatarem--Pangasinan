{
    "version": 2,
    "buildCommand": "pip install uv && uv sync && pip install dotenv && pip install -r requirements.txt",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/app"
        }
    ]
}