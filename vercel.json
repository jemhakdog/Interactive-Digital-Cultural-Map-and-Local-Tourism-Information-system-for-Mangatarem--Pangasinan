{
    "version": 2,
    "buildCommand": "pip install uv && uv pip install -r requirements.txt --system",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/flask_app"
        }
    ]
}