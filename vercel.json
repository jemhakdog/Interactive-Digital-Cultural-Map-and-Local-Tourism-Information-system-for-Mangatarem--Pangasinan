{
    "version": 2,
    "buildCommand": "pip install uv && uv sync && pip install python-dotenv && pip install -r requirements.txt",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/app"
        }
    ]
}